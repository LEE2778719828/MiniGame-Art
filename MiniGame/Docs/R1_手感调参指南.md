# R1 手感调参指南（DA_NightFeelTuning_Default）

调 R1 手感只需要动一个资产：

```
/Game/Night/Course/Data/DA_NightFeelTuning_Default
```

它挂在 `BP_NightCoursePawn` 的 `FeelStub.Tuning` 上，`BeginPlay` 时把表里的值套到 Feel 组件与主角。留空则退回代码默认值。

> **边界**：这张表只管 R1 的手感。起始魂量、关卡长度、石距、敌人分布归 canonical
> `DA_Course`，敌人 Blueprint 由其中的 `FoeActorMap` 决定。

---

## 一、三种改法


| 方式            | 怎么做                                                       | 何时生效    | 适合       |
| ------------- | --------------------------------------------------------- | ------- | -------- |
| **改资产**（推荐）   | Content Browser 双击 `DA_NightFeelTuning_Default`，改完 Ctrl+S | 下次 Play | 定稿、进版本库  |
| **PIE 控制台现调** | ``` 打开控制台，`Night.Feel.Set <项> <值>`                        | 立即      | 找手感、来回试  |
| **改表 + 热重载**  | PIE 跑着时改资产，然后 `Night.Feel.Reload`                         | 立即      | 不想重开 PIE |


**不需要蓝图。** 这张表是 DataAsset，双击就是一个属性面板，和蓝图的 Details 面板长一样，但不用编译。

### 调参回路

```
Play → Night.Feel.Set Early 700    // 现调，立刻能感觉到
     → 反复试到满意
     → Night.Feel.Save             // 把当前运行值写回表并存盘
```

反过来，在 Content Browser 里改了表而 PIE 正开着：`Night.Feel.Reload`。

随时用 `Night.Feel.Status` 打印当前全部生效值，最后一行会显示挂的是哪张表。

---



## 二、参数逐条



### Grace 空档期 —— 「不按键多久开始掉魂」


| 参数                       | 默认    | 含义                        |
| ------------------------ | ----- | ------------------------- |
| `JumpWindowMs`           | 280   | 跳跃落地后的宽限，这段时间内发呆不受罚       |
| `AttackWindowMs`         | 260   | 斩击节点的宽限                   |
| `bGraceWaitsForAnim`     | true  | 裁定 R-007：宽限从**动作播完之后**才起算 |
| `bUseTutorialWindows`    | false | 教学放宽档总开关                  |
| `TutorialJumpWindowMs`   | 700   | 开教学档时替换上面两项               |
| `TutorialAttackWindowMs` | 520   | 同上                        |


关键在于**窗口本身永不关闭**。这两个值不是「按晚了就失败」的截止线，按对任何时候都算命中。它们只决定「拖多久开始呼吸扣魂」。

`bGraceWaitsForAnim` 打开时，实际宽限 = 动画剩余时长 + 上面的 ms。这样斩击动画播 367ms 的时候不会因为动画长就被判发呆。关掉可以对比旧口径（落地即起算）。

**调大** → 节奏松、容错高；**调小** → 逼玩家连打，紧张感强。

### CatchUp 提前与追赶 —— 「按早了怎么办」


| 参数                     | 默认  | 含义                                 |
| ---------------------- | --- | ---------------------------------- |
| `EarlyAcceptMs`        | 550 | 早于空档期这么多 ms 内的输入进缓存；更早则忽略（不扣魂、不断连） |
| `CatchUpPlayRate`      | 1.5 | 命中缓存时，没走完的石间位移按此倍率加速播完             |
| `MaxCatchUpCompressMs` | 300 | 加速最多吃掉多少提前量，超出不再压缩                 |
| `MaxBufferedInputs`    | 3   | 缓存队列长度                             |


这四个决定「连打顺不顺」。你之前反馈的「按 Q 后立刻按 E 有丝滑衔接」就是这套在起作用：提前的那一下不丢，存起来，同时把正在走的位移加速播完追上节奏。

- `EarlyAcceptMs` **调大** → 更宽容，抢拍也认；调小 → 必须踩点。
- `CatchUpPlayRate` **调大** → 追赶更急，动作会明显加速（动画倍率同步乘上去）；调到 1 = 不追赶，提前的输入要等位移走完才生效。
- `MaxCatchUpCompressMs` 是安全阀，防止提前太多导致动作快到看不清。



### Soul 扣魂


| 参数                     | 默认   | 含义                                |
| ---------------------- | ---- | --------------------------------- |
| `BreathDecayPerSecond` | 0.55 | 空档期过后每秒掉多少魂，直到玩家行动（裁定 R-006 呼吸扣血） |
| `bEnableBreathDecay`   | true | 关掉则发呆完全免费，仅调试用                    |
| `SoulPenaltyHazard`    | 7    | 在跳跃节点按错的扣魂                        |
| `SoulPenaltyEnemy`     | 5    | 在敌人节点按错的扣魂                        |
| `HitInvulnMs`          | 600  | 受罚后的无敌，期间不重复扣，防止连按被连罚             |


按裁定 R-006，只有两种情况掉魂：**按错**（一次性，取上面两个值）和**发呆**（持续，按 `BreathDecayPerSecond`）。按晚了不罚。

> **待收口**：Director 另有 `Config->WrongPenalty / MissPenalty` 一条路径，和 `SoulPenaltyHazard/Enemy` 覆盖范围重叠。改这里只影响 FeelStub 自判那条。需要和 R2 统一到一处。



### Anim 动作


| 参数                   | 默认    | 含义                                       |
| -------------------- | ----- | ---------------------------------------- |
| `JumpAnimRate`       | 1.0   | 跳跃动作基准倍率（<1 慢、>1 快）                      |
| `AttackAnimRate`     | 1.0   | 斩击动作基准倍率                                 |
| `bAnimDrivenAdvance` | false | 开启后石间位移时长由动画锚点决定，不再用 R2 的 `AdvanceSpeed` |
| `JumpAnchorMs`       | 331   | 跳跃落地帧（倍率 1.0 下），仅动画驱动模式生效                |
| `AttackAnchorMs`     | 179   | 斩击接触帧，同上                                 |


倍率是**基准**，追赶加速在此之上再乘，所以改它不影响双时钟的压缩逻辑。

`bAnimDrivenAdvance` 是你提的那个方案：让位移时长等于动画到锚点的时长，这样倍率一改两者一起缩放、永远同步，一拍绝不会短于动作本身。代价是移动速度随石距浮动，石距不再影响节奏。默认关着，用 `Night.Anim.Drive 1` 开了对比过再决定。

**`Night.Anim.Drive 1` 重开 PIE 就没了，这是对的。** 它只写正在跑的那个 Pawn。下一局 `BeginPlay` 会走 `ApplyTuning`，把这张表里的 `bAnimDrivenAdvance` 又拷回 Pawn 上——表里还是 false，就又变回按 `AdvanceSpeed` 走了。想留住：

- PIE 还开着 → 打 `Night.Feel.Save`。它把当前运行值整套写回 `DA_NightFeelTuning_Default` 并存盘，`bAnimDrivenAdvance` 也在里面。
- 或者停掉 PIE，双击 `DA_NightFeelTuning_Default` 手动勾上，再 `Ctrl+S`。

`JumpAnchorMs` / `AttackAnchorMs` 在面板上带 `EditCondition`，勾上 `bAnimDrivenAdvance` 之前是灰的，改不动。

**为什么要开它。** 位移走的是匀速插值，耗时 = 石距 ÷ `AdvanceSpeed`（`DA_Course`，默认 1400 cm/s），而动画长度是固定的。分水岭是 `1400 × 0.331 ≈ 463cm`：

| 石距 | 位移耗时 | 现象 |
|---|---|---|
| 斩击 140–160cm | 100–114ms | 早于 Contact 179ms，人先到、刀后到 |
| 跳跃 420cm | 300ms | 和 Land 331ms 基本对上 |
| 原子间过渡跳 520cm | 371ms | **晚于 Land，落地后还在滑**——"飘过去" |

开了动画驱动之后跳固定 331ms、斩固定 179ms，与石距无关，位移永远停在关键帧那一刻。速度反过来随石距浮动（520cm→1571 cm/s、160cm→894），相机滞后最大 1571÷12≈131cm，还在 `CameraLagMaxDistance` 200 以内，不用跟着调。

### Debug 调试


| 参数             | 默认   | 含义                     |
| -------------- | ---- | ---------------------- |
| `bLogJudge`    | true | 每次判定写日志                |
| `bLogHudLines` | true | 屏显文字同时写日志（屏显一闪而过看不清时用） |


---



## 三、控制台速查

```
Night.Feel.Status                    打印当前全部生效值 + 挂的哪张表
Night.Feel.Set <项> <值>             现调，见下表
Night.Feel.Reload                    从表重新套用（改了表不想重开 PIE）
Night.Feel.Save                      把当前运行值写回表并存盘

Night.Anim.Status                    动作倍率与剩余时长
Night.Anim.Rate <Jump|Attack> <倍率>  改基准倍率
Night.Anim.Drive <0|1> [跳锚点] [斩锚点]  切动画驱动位移
Night.Anim.Mesh <抬升Z> [Yaw] [缩放]      现调主角落脚高度 / 朝向 / 大小
```

`Night.Feel.Set` 可用的项：

```
Jump  Attack  Tutorial  Early  CatchUp  MaxCompress
Hazard  Enemy  Invuln  Soul  Breath  BreathOn
LogHud  GraceWaitsAnim
```

`Soul` 是直接改当前魂量，用来把血量拨回满值继续测，不进表。

---



## 四、建议的调参顺序

1. **先修帧率**。当前 PIE 只有 15fps 左右，一帧就是 66ms，280ms 的空档期只有 4 帧。在这个帧率下十几毫秒的差别是感觉不到的，调再细都是白调。
2. `AttackAnimRate` **/** `JumpAnimRate` —— 先让动作看着顺眼，这是所有节奏的基准。
3. `EarlyAcceptMs` **+** `CatchUpPlayRate` —— 调连打的顺滑度，这一组对「爽」的贡献最大。
4. `JumpWindowMs` **/** `AttackWindowMs` —— 调整体松紧。
5. `BreathDecayPerSecond` —— 最后调惩罚力度，前面没定死之前调这个没有参照。

每改完一轮 `Night.Feel.Save` 落档，想留多套预设就复制资产另存。

---



## 五、改哪个资产（TA 对接面）

下面 **§5.2 是目录**：先从表里找到「想调什么」，右边「详解」列就是本节序号。§5.3 起是对应说明，不要从上往下当连续教程读。

### 5.1 为什么 `BP_NightCoursePawn` 的事件图是空的

因为逻辑全在它的父类里。这是**代码蓝图**——父类是 C++ 的 `ANightCoursePawn`，输入、判定、位移、动画播放都在那边。蓝图这层只是个配置壳：Details 面板里的值会生效，事件图空着是故意的。

这么分是为了避免出现两套实现。逻辑一旦有一半在 C++一半在蓝图，改 C++ 的人不知道蓝图里还有一份，两边会静静地对不上。所以口径是：**蓝图只放数据，不放逻辑。**

需要新的可调项时告诉 R1，在 C++ 里加 `EditAnywhere` 暴露出来，比在蓝图里现搭一套快，也不会分叉。

### 5.2 改什么去哪里（目录）


| #   | 想调               | 去哪                           | 位置                                            | 详解                       |
| --- | ---------------- | ---------------------------- | --------------------------------------------- | ------------------------ |
| 1   | 手感数值（空档、缓存、扣魂）   | `DA_NightFeelTuning_Default` | 双击资产改表                                        | 第一～四节                    |
| 2   | 相机（臂长、偏移、角度、FOV） | `BP_NightCoursePawn`         | 组件列表选 `SpringArm` / `Camera`                  | [§5.3](#53-相机臂长偏移角度-fov) |
| 3   | 相机跟拍缓动           | `BP_NightCoursePawn`         | 选 `SpringArm` → `Lag` 分类                      | [§5.4](#54-相机跟拍缓动)       |
| 4   | 跳 / 斩的镜头冲击       | `BP_NightCoursePawn`         | `Night｜Camera｜Feel`                           | [§5.5](#55-跳--斩的镜头冲击)    |
| 5   | 失误震屏幅度           | `BP_NightCoursePawn`         | `Night｜Camera｜Feel` → Fail Camera Shake Scale | [§5.6](#56-失误震屏)         |
| 6   | 夜关血条位置 / 缩放      | `BP_NightCourseHUD`          | 类默认值 → `Night｜HUD`                            | [§5.7](#57-夜关血条)         |
| 7   | 动作 clip、倍率、锚点    | `BP_NightCoursePawn`         | `Night｜Anim`                                  | [§5.10](#510-动画那边的硬约束)   |
| 8   | 主角落脚高度、朝向        | `BP_NightCoursePawn`         | `Night｜Art`                                   | [§5.9](#59-两个坑)          |
| 9   | 主角网格、材质、缩放       | `BP_NightHero`               | `Night｜Art｜Visual`                            | [§5.9](#59-两个坑)          |
| 10  | 待机姿势、动作混合        | `ABP_NightHero`              | AnimGraph                                     | [§5.10](#510-动画那边的硬约束)   |
| 11  | 斩击 / 跳跃的特效音效     | `Slash` / `Jump`             | 通知轨已标好时间点                                     | [§5.10](#510-动画那边的硬约束)   |
| 12  | 动作收尾融回 idle 的软硬   | `BP_NightCoursePawn`         | `Night｜Anim` → Action Blend Out Seconds       | [§5.11](#511-动作收尾衔接)     |
| 13  | 触控热区（左跳右斩）       | `BP_NightCourseHUD`          | 类默认值 → `Night｜HUD｜Touch`                      | [§5.12](#512-触控热区)       |
| 14  | 热区半透明框 / NOW 调试字 | `BP_NightCourseHUD`          | 类默认值 → `Night｜HUD｜Debug`                      | [§5.12](#512-触控热区)       |
| 15  | 砍怪掉落图标飞向鱼嘴       | `BP_NightCourseHUD`          | 类默认值 → `Night｜HUD｜Drop`                       | [§5.13](#513-掉落飞图标)      |
| 16  | 砍击 / 受击 / 掉落音效     | `BP_NightCourseHUD`          | 类默认值 → `Night｜HUD｜SFX`                        | [§5.14](#514-跑酷音效)       |
| 17  | 连击「数字 + 連」位置字体   | `WBP_Combo` / `WBP_NightHUD_Multi` | Designer 里摆，C++ 只灌数字                  | [§5.15](#515-连击数字)       |


保存、何时生效见 [§5.8](#58-怎么保存何时生效)。键盘仍是 Q 跳、E 斩。手机点的是游戏画面里的两块热区（默认左下半跳、右下半斩），比例写在 `Night｜HUD｜Touch`，**默认不画色块**。要对齐热区，临时勾 `bShowTouchZones`（见 §5.12）。空白处点了不算。引擎虚拟摇杆已关掉，不会和热区抢点。

### 5.3 相机（臂长、偏移、角度、FOV）

对应目录第 2 行。打开 `BP_NightCoursePawn`，在左上角**组件列表**里选中 `SpringArm` 或 `Camera`，改它们自己的属性，所见即所得：


| 想调     | 选哪个组件       | 属性                   | 当前值（编辑器 CDO） |
| ------ | ----------- | -------------------- | ------------ |
| 镜头拉远拉近 | `SpringArm` | Target Arm Length    | 130          |
| 俯角     | `SpringArm` | Rotation 的 **Pitch** | −11°         |
| 画面偏移   | `SpringArm` | Socket Offset        | (25, 0, 150) |
| 视野广窄   | `Camera`    | Field Of View        | 70           |


Yaw / Roll 保持 0。数改完请同步改这张表。

以前这几个值在 Details 面板的 `Night｜Camera` 分类下有一份**镜像属性**，开局会覆盖组件上的设置——在组件上改不生效也不报错。那三个属性已经删掉了，现在组件是唯一真相。

`SpringArm` 只继承 Yaw（`Inherit Pitch/Roll` 关着），所以转弯时镜头跟着摆，但角色的俯仰翻滚不会带歪画面。这三个开关别动。

还有四项开局仍会被代码强制写回，在组件上改**不生效**：`Do Collision Test`（关，否则桥面会把镜头顶开）、两个组件的 `Use Pawn Control Rotation`（关，本作没有视角输入）、以及 `Camera` 的那一组曝光覆盖（锁成手动，夜景不能自动适应）。要动这四项得改 C++ 的 `EnforceCameraInvariants`。

**对概念图：只改上面四个，按这个顺序，一次动一个。** 这轮别动 Lag（§5.4），也别动 `Night｜Camera｜Feel`（那是 §5.5 / §5.6 的跳斩冲击和失误震屏）。

对照竖屏概念图（后上方高俯视、角色压在下 1/3、路往画面上方延伸），可以先从这组试起，再按下面 1～5 微调：


| 项                 | 先试           |
| ----------------- | ------------ |
| Target Arm Length | 130          |
| Pitch             | −11          |
| Socket Offset     | (25, 0, 150) |
| Field Of View     | 62           |


1. **Pitch（决定「像不像那张图」）**
  选中 `SpringArm`，Relative Rotation 的 Pitch 往更负收。还是太平 → 试 −52；已经像俯视地图 → 回到 −42。别一下拉到 −60，角色会变成脚底贴图。
2. **Arm Length（决定前面能看见几块石）**
  俯角变陡以后，同样臂长会显得贴在脑后、路变短，所以要加长。角色太小、石太远 → 减 50；前面空档不够、怪看不清 → 加 50。
3. **Socket Offset Z（决定角色在画面里偏上还是偏下）**
  概念图角色在**下 1/3**。Z 加大 = 镜头抬高 = 角色往屏幕下方掉。角色还在画面中间 → Z 加大；脚被裁掉 → Z 减小。
4. **Socket Offset 的 X / Y**
  X 沿臂前后推，Y 是左右。概念图水平居中则 **Y 用 0**。先别用 X 代替臂长。
5. **FOV 最后动**
  略收（例如 62）更像「路被压成一条、两边不鼓」。两边太空、像望远镜 → 加；鼓、路发弯 → 减。别用 FOV 代替俯角。

**怎么改才看得到**

打开 `BP_NightCoursePawn` → 左上角组件列表点 `SpringArm` / `Camera`，改它们自己的属性。不要改 Details 里 `Night｜Camera｜Feel` 那几项。

判断构图请看 **中间那块竖屏游戏区**，不要看带黑边的整窗。

**最省事：**

1. Play
2. 大纲选中 `NightCoursePawn`（这一局的实例）
3. 组件列表里拧 `SpringArm` / `Camera`——画面立刻变，方便找数
4. 把四个数记下来
5. **停掉 PIE**（实例上的改动不会写回蓝图，不停就丢）
6. 打开 Content Browser 里的 `BP_NightCoursePawn`，把记下的数填回组件
7. `Ctrl+S`，再 Play 验证

不要在 PIE 视口里当「改好了」——那只是这一局。蓝图存盘后必须再开一局才算数。对一下：角色在竖屏下 1/3、路往画面上方延伸、还能看清后背和刀（Pitch 别过 −55）、前面至少能看见下一块空档 / 带怪石。

### 5.4 相机跟拍缓动

对应目录第 3 行。选中 `SpringArm`，Details 里有个 **Lag** 分类，五项都在那儿。这是镜头"重量感"的主要来源。


| 属性                         | 默认  | 作用                     |
| -------------------------- | --- | ---------------------- |
| Enable Camera Lag          | 开   | 位移缓动的总开关               |
| Camera Lag Speed           | 12  | **主旋钮。** 越低越拖越有重量，越高越贴 |
| Enable Camera Rotation Lag | 开   | 转向缓动的总开关               |
| Camera Rotation Lag Speed  | 10  | 越低转弯越绵                 |
| Camera Lag Max Distance    | 200 | 安全钳制，不是观感项，别当手感旋钮调     |


**为什么需要它。** `NightCoursePawn::Tick` 里石间位移走 `VInterpConstantTo`——匀速出发、到点急停，没有加减速；而朝向是**直接赋值**，压根没插值。所以不开缓动的话，起步和落地都是硬切，转弯是瞬间掰头。缓动就是把这两件事补成有始有终的跟拍。

**怎么估滞后量。** 匀速段的滞后距离约等于 `AdvanceSpeed ÷ Camera Lag Speed`。默认 1400 ÷ 12 ≈ 117cm。因为镜头在角色正后方，这个滞后主要落在**景深**上：移动时角色显得略远，读起来就是"镜头在追"。想让追赶感更明显就降 Speed，但降太多角色会缩得厉害。

**调参方向。**

- 太黏、拖尾巴 → 提高 Camera Lag Speed（试 16、20）
- 太硬、没重量 → 降低（试 8）
- 只有转弯不满意 → 单独动 Camera Rotation Lag Speed，别动位移那一项
- **Max Distance 不要调小**来"减少滞后"。它是钳制而非阻尼，调小会让镜头在匀速段被硬顶住，产生突兀的贴合感。想减滞后请提高 Speed

**别把 Max Distance 关掉（设 0）。** 开局 `StartNight` 会把角色硬传送到 0 号石，没有这个钳制，镜头会从原点横扫过整张地图。引擎在 `UpdateDesiredArmLocation` 里把滞后目标钳在真实目标附近，正好顺带吃掉这次传送——所以不需要额外写传送重置逻辑。

### 5.5 跳 / 斩的镜头冲击

对应目录第 4 行。跳和斩会在关键帧给镜头一记冲击。改幅度在 `BP_NightCoursePawn` 的 Details 面板，`Night｜Camera｜Feel` 分类下（注意这几项是 Pawn 属性，不在 SpringArm / Camera 组件上，和 §5.3 不是同一处）：


| 属性                         | 默认  | 作用                        |
| -------------------------- | --- | ------------------------- |
| Takeoff Fov Kick Deg       | 5   | 起跳瞬间 FOV 张开的度数，正值张开       |
| Land Boom Dip Cm           | −14 | 落地瞬间镜头下沉的厘米数，负值向下         |
| Attack Fov Kick Deg        | −6  | 斩击接触瞬间的 FOV 变化，负值收紧成推近打击感 |
| Camera Kick Recovery Speed | 9   | 三者回零的速度，越低余韵越长            |


**时刻不在这里配。** 触发时刻是运行时从动画的通知轨上读的：`Takeoff` / `Land` 取自当前 `JumpAnim` 指向的片子，`Contact` 取自 `Slash`。所以美术把动画重新配时，相机自动跟着走，不需要改任何数值。

**但换片子会把冲击一起换掉。** 通知是挂在片子上的，不是挂在属性上。把 `JumpAnim` 指到一条没打通知的新片，起跳不张 FOV、落地不沉镜头，静默失效。换片必须重打 `Takeoff` / `Land`（量法见 §5.10）。

代价是**通知名不能随便改**。C++ 里按 `Takeoff` / `Land` / `Contact` 这三个名字匹配，在动画里把某个通知改名，那一记冲击就会静默消失——不报错。要改名得同步改 `NightCoursePawn.cpp` 里的 `NightCameraNotify`。

想彻底关掉某一记，把对应幅度设成 0 即可。

**和追赶加速的关系。** 负反应缓存命中时动作会被压缩加速，已排队的冲击时刻会按同一倍率一起缩，所以落地下沉不会跑到角色落地之后才发生。

**别在事件图表里运行时 Set 组件的 FOV 或 Socket Offset。** 冲击生效期间代码每帧都在写这两项，运行时的 Set 会被冲掉；空闲帧不写，所以你可能会看到"有时生效有时不生效"。基准值请在组件上改（见 §5.3），代码会以组件的值为基准往上叠加。

### 5.6 失误震屏

对应目录第 5 行。错键和 Miss 走 `PlayFailFeedback` → `ClientStartCameraShake`，只乘 `Fail Camera Shake Scale`，**不替代** §5.5 那三记 FOV / 下沉。


| 属性                      | 默认                      | 作用                      |
| ----------------------- | ----------------------- | ----------------------- |
| Fail Camera Shake       | `CS_CameraShake_Return` | 播哪一个 CameraShake        |
| Fail Camera Shake Scale | 1                       | 幅度，0 关掉。手感上先试 0.3 / 0.1 |


资产在 `Content/Night/Course/Camera/CS_CameraShake_Return`，是 **Sequence Camera Shake**（播一段相机动画），不是左右晃的 Perlin 震荡。当前这段动画主要是 FOV 推近，所以按错时画面像「放大一下再回去」——把 Scale 从 1 降到 0.1 只会变弱，不会变成左右抖。

- 只要强弱：`BP_NightCoursePawn` → `Night｜Camera｜Feel` → **Fail Camera Shake Scale**，`Ctrl+S`，停 Play 再开。
- 要改成左右轻晃：换一个带 Location / Rotation Oscillation 的 CameraShake，或让 TA 改 Sequence 里的曲线。
- 彻底关掉：Scale 设 0，或把 Fail Camera Shake 清空。



### 5.7 夜关血条

对应目录第 6 行。血条分两层：样子在 Widget 里，屏幕位置在 HUD 蓝图里。逻辑仍在 C++ 父类，蓝图只改数。


| 资产                       | 路径                                 | 改什么                                          |
| ------------------------ | ---------------------------------- | -------------------------------------------- |
| `WBP_HealthBar`          | `Content/Night/Course/Blueprints/` | 底 / 填充 / 火苗的贴图与设计尺寸                          |
| `BP_NightCourseHUD`      | 同上                                 | 屏幕上的左右、缩放、距顶                                 |
| `BP_NightCourseGameMode` | 同上                                 | HUD Class 必须指向 `BP_NightCourseHUD`，否则类默认值不生效 |


夜关 HUD 现在是组合壳：`WBP_NightHUD_Multi` 里嵌着血条、背包、连击。C++ 只把 Multi 加到屏幕，再按实例名找 `WBP_HealthBar`。打开 `BP_NightCourseHUD` → 工具栏 **类默认值** → `Night｜HUD`：


| 属性                     | 默认         | 作用                                            |
| ---------------------- | ---------- | --------------------------------------------- |
| Main HUD Scale         | 0.5        | 整块组合 HUD 缩放，绕左上角，左右起点不跟着跑                      |
| Main HUD Left Margin   | 80         | 相对**游戏画面**左边界的偏移（竖屏黑边不算）                      |
| Main HUD Top Margin    | 24         | 相对游戏画面顶边的偏移                                   |
| Health Bar Design Size | (800, 240) | 和 `WBP_HealthBar` 底图宽度对齐，给 `SetHealth` 算填充用。没改底图宽度就别动 |


`Left Margin = 0` 是中间那块竖屏的左沿，不是整个编辑器窗口的左沿。UMG 铺的是整窗，C++ 每帧用 HUD Canvas 的信箱算出游戏区再叠这两个 Margin。连击 / 掉落 / 热区见 §5.12～§5.15，不要和血条 Margin 混在一块拧。

**不要在 Designer 里拖** `Img_Fill` **/** `Img_Glow`**。** `SetHealth` 每帧会改写它们的尺寸和位置，拖了进 Play 也会被盖掉。只改 `Img_Background` 的贴图，或改上面三个 Margin / Scale。

燃烧进度是 TA 在 `WBP_HealthBar.SetHealth` 里做的，不是只挪火苗：

1. `HealthPercent = Soul / 100`（C++ 每帧把 `Feel->Soul` 灌进去）
2. `FillWidth = HealthPercent × FullBarWidth`
3. `FillLeftX = FullBarWidth − FillWidth` → 剩余血贴在条的**右边**，火苗贴在剩余段左沿
4. `Img_Background` 始终全宽

满血时 Fill 和底一样长。Play 站一会儿会看到 `BREATHING` 掉魂，Fill 变短、集中在右侧，这是剩余血，不是坏了。要把血拨满再看：控制台 `Night.Feel.Set Soul 100`。

类默认值 `Ctrl+S` 后要**停掉 PIE 再 Play** 才看得到。Play 时在大纲里改 `NightCourseHUD0` 是这一局的实例，停掉就丢。

### 5.8 怎么保存、何时生效

**改 Details 面板上的数字（蓝图 / DataAsset / 动画通知轨）：**`Ctrl+S` **就够，不用编译。**

确认落盘的标志：资产标签页上的星号消失，右下角变成「所有已保存」。只按 `Ctrl+S` 存的是**当前焦点那个资产**；同时改了好几个，用 `Ctrl+Shift+S` 全存。


| 改了什么                                                            | 保存                           | 何时看见                                   |
| --------------------------------------------------------------- | ---------------------------- | -------------------------------------- |
| `BP_NightHero` / `BP_NightCoursePawn` / `BP_NightCourseHUD` 的属性 | `Ctrl+S`                     | **停掉 PIE 再 Play**。开局会拷一次值，正在跑的那一局不会跟着变 |
| `DA_NightFeelTuning_Default`                                    | `Ctrl+S`                     | 下次 Play；PIE 正开着则再打 `Night.Feel.Reload` |
| 动画上的 Notify                                                     | `Ctrl+S`                     | 下次 Play                                |
| `ABP_NightHero` 的图                                              | 先 **编译**（工具栏编译按钮），再 `Ctrl+S` | 下次 Play                                |


不要在 PIE 视口里点角色改 Details——那是这一局的实例，不写回蓝图，停掉就丢。始终打开 Content Browser 里的那个资产改。

控制台现调（`Night.Feel.Set`、`Night.Anim.Mesh`）立刻看得见，但**不写磁盘**。满意之后还是要回到对应资产改同一组数，再 `Ctrl+S`。手感表有捷径：`Night.Feel.Save` 会把当前运行值写回 `DA_NightFeelTuning_Default` 并存盘。缩放没有对应命令，PIE 里试完要把数填进 `BP_NightHero` 的 `HeroScale`。

### 5.9 两个坑

对应目录第 8、9 行（主角落脚 / 网格为什么不能乱改资产）。

`BP_NightHero` **只有五个属性会生效。** 它不是被 spawn 的 Pawn，只是个"外观配置载体"，开局由 `NightCourseHost::WireFeelFromPlayer` 把值拷到真 Pawn 上。拷的只有 `HeroSkeletalMesh`、`HeroStaticMesh`、`HeroMaterial`、`HeroScale`、`HeroPivotOffsetCm` 这五个。在它上面改相机、改动作倍率、改落脚偏移——**不报错，也不生效**。那些要改在 `BP_NightCoursePawn` 上。

同一类静默失效以前在相机上也有一份（镜像属性覆盖组件），已经消掉了，见 §5.3。

`.uasset` **合不了。** 二进制资产没有三方合并，两个人同时改同一个就是丢一边的工作。动上面这些之前先说一声谁占哪个。

**试完值改回去，也要再存一次。** 存过一个试验值之后又在面板里改回原值（或者按撤销），那次回退同样要 `Ctrl+S`，否则盘上留着的还是试验值——现在 Play 用的是内存里的新值，看起来一切正常，重开编辑器才变回去。这种不一致 `git status` 只会显示资产被改过，看不出改成了什么。要确认改了什么值只能在编辑器里打开对比。

### 5.10 动画那边的硬约束

对应目录第 7、10、11 行（clip / 锚点、AnimGraph、特效音效通知）。

`ABP_NightHero` 的 TargetSkeleton 是 `SK_Hero_Skeleton`，建完即固定，改不了。交付里那个 `ABP_zhujue` 绑的是 `SK_zhujueSitting_Skeleton`，装不到我们的主角上，得在 `ABP_NightHero` 里做。

待机现在是 `Idle_Breath`（1.633s，首末帧髋高一致可循环），换掉了原先的 `Sitting` 坐姿占位。它只是序列播放器那一个节点上的资产，待机不参与判定、不带通知，换它不牵连锚点。

特效音效不需要动画蓝图：标记已经全部标好，在标记处放自执行型 Notify（`Play Sound` / `Play Niagara Effect`）就能直接出效果。


| 片段                                 | 标记                 | 时间              | 用途         |
| ---------------------------------- | ------------------ | --------------- | ---------- |
| `Slash`                            | `Contact`          | 0.179s          | 命中         |
| `Jump`（当前跳跃）                       | `Takeoff`          | 0.045s          | 蹬地烟尘       |
| `Jump`（当前跳跃）                       | `Land`             | 0.331s          | 落地         |
| `Jump_noknife2`                    | `Takeoff` / `Land` | 0.100s / 0.266s | 已下线，留作量法参照 |
| `Jump_noknife2_fast`               | `Takeoff` / `Land` | 0.064s / 0.167s | 快版备用，未启用   |
| `Slash_fast_Armature_Armature_kan` | `Contact`          | 0.083s          | 快版备用，未启用   |


这些时间是逐帧实测的（`Tools/ProbeAnimNotifyPoints.py`），和代码里的 `AttackAnchorMs` / `JumpAnchorMs` 同源，别改。

**量法，换跳跃动画时照做。** 脚本按 5ms 一格采样髋部和脚尖高度，两者都以第 0 帧（站定）为基准：

- `Takeoff` = 脚尖离地前的最后一格。脚尖抬过 2cm 算离地，前一格就是蹬地那一瞬。
- `Land` = 脚尖重新落回 2cm 以内的第一格。
- `JumpAnchorMs` 取和 `Land` 同一个时刻——位移在落地那一刻结束，之后是收尾。旧片两者都是 266ms，新片都是 331ms。

别拿全片的脚尖最低点当地面：末帧常常把脚背绷得比站定还低，用它当基准会让每一格都显示离地。

**怎么打上去。** 这三个标记必须是**骨架通知**——只有名字，背后不挂通知对象。C++ 匹配的是通知名字符串，而对象型通知的名字是从类名推导的，会在某次刷新后被刷成 `AnimNotify`，两条撞成同名后冲击一起静默消失。

历史上（第 29–31 轮，提交 `a7d4517` / `1f69ef5`）这三个标记是纯手工在动画编辑器里点出来的，仓库里从来没有、也写不出对应脚本。

**推荐走两步，最省事，也不用在菜单里找「添加通知」：**

1. 先用 MCP 按实测毫秒放一条占位：`manage_asset_anim_sequence` 的 `add_notify`（`notifyName` 随便填，时间要准）。它只能建对象型，但时间是精确的，省掉手拖对不准的麻烦。
2. 再在动画编辑器里右键那条标记 → **替换为通知** → 从列表里选 `Takeoff` / `Land` / `Contact`。替换保留原时间，替完 `notifyClass` 消失，形式就对了。

`Takeoff` / `Land` / `Contact` 都已注册在 `SK_Hero_Skeleton` 上，列表里直接选，不用新建。

**纯手工路线的两个坑：**

- 左侧面板上「通知」和轨道名 `1` 右边的 ▼ 按钮里**只有轨道级操作**（添加/删除通知轨道），没有添加通知。点它只会凭空多一条空轨道 `2`，要用同一个 ▼ 里的删除通知轨道清掉。
- 「添加通知」在**轨道横带本身**的右键菜单里，必须点在和已有标记完全同高的那条细带上（约 15px 高）。差一点点落到下方空白，弹出来的就是序列帧编辑菜单（移除/插入帧、在第 N 帧处重置为零）——看到它就是点偏了。

打完用 MCP 验一眼：`get_asset_anim_sequence` 读回来的通知**不应该**带 `notifyClass` 字段，带了就是对象型，还得替换。

`Slash` 上 300ms 处武器手速度还有一个 1518cm/s 的峰，那是**收招回摆不是第二次命中**，在那儿放打击音会变成一刀两响。想做刀光用带时长的 Notify State，区间取 133–233ms。

<a id="511-动作收尾衔接"></a>

### 5.11 动作收尾衔接

对应目录第 12 行。跳 / 斩是一次性动作，播完要回 `Idle_Breath`。这一段的软硬在 `BP_NightCoursePawn` 的 `Night｜Anim` 分类下：

| 属性 | 默认 | 作用 |
|---|---|---|
| Action Blend Out Seconds | 0.15 | 动作末尾融回 idle 的时长。**0 = 一帧硬切**（原来的行为） |
| Action Blend In Seconds | 0 | 融入动作的时长，建议保持 0 |

**为什么默认要给淡出。** 原来两端都写死 0，动作末帧到 idle 是一帧跳变。短石距尤其明显：斩击 114ms 就到位了，剩下 250ms 站在石头上把片子播完，然后"啪"一下换姿势。给 0.15s 之后这段死尾变成融入 idle。

**上限是片子算出来的，不是随便定的。** 淡出从 `片长 − 淡出时长` 开始，必须落在关键帧**之后**，否则会在落地 / 接触之前就开始淡出：

| 片段 | 片长 | 关键帧 | 关键帧之后的余量 |
|---|---|---|---|
| `Jump` | 492ms | Land 331ms | 161ms |
| `Slash` | 367ms | Contact 179ms | 188ms |

所以属性上限钳在 0.16s（按 Jump 那一档定的）。代码里还会再按当前片子夹一次，换成更短的片子也不会把关键帧淡掉。淡入同理夹在关键帧之前。

**淡入为什么留 0。** `Jump` 的 `Takeoff` 在 45ms，淡入只要有实际长度，蹬地那一刻还在爬权重，起跳会发软。

**不影响的东西**：淡出不改蒙太奇总长，所以 `bGraceWaitsForAnim` 的空档期起算点不变；相机冲击读的是片子上的通知时间，也不受淡出影响。

调法：嫌软 → 降到 0.10；还能看到跳变 → 加到 0.16。`Ctrl+S` 之后**停掉 PIE 再 Play**。

<a id="512-触控热区"></a>

### 5.12 触控热区

对应目录第 13、14 行。热区是**游戏画面（信箱内竖屏）的 0–1 比例**，不是整窗像素，也不是 UMG 控件。PC 键盘 Q / E 不受影响；手机 `BindTouch(IE_Pressed)` 走同一套命中。分叉选路时，左热区 = 左路、右热区 = 右路。

打开 `BP_NightCourseHUD` → **类默认值** → `Night｜HUD｜Touch`：


| 属性                | 默认 (X, Y, W, H)     | 作用                         |
| ----------------- | ------------------- | -------------------------- |
| Jump Touch Norm   | `(0, 0.5, 0.5, 0.5)`   | 左下半屏：跳                     |
| Attack Touch Norm | `(0.5, 0.5, 0.5, 0.5)` | 右下半屏：斩                     |


四个分量都是 **0–1**：X / Y 是热区左上角相对游戏区的位置，W / H 是宽高。例如只想用底部 1/3：Y=0.67、H=0.33。两块可以重叠；重叠时**先测跳再测斩**，左边优先。

`Night｜HUD｜Debug`：


| 属性                 | 默认    | 作用                                          |
| ------------------ | ----- | ------------------------------------------- |
| Show Debug Hud     | false | 旧的 `NOW: ATTACK/JUMP`、ROUTE、阶段字。出货关着         |
| Show Touch Zones   | false | 半透明蓝（跳）/ 红（斩）矩形，专门用来对热区。对完再关                 |


对热区的回路：勾上 `Show Touch Zones` → Play → 看色块是否盖住想点的位置 → 改 `Jump/Attack Touch Norm` → `Ctrl+S` → **停掉 PIE 再 Play**。色块和真实命中是同一套比例，画出来点得到。

**坑：**

- 新增 / 改过这两个分类的 `UPROPERTY` 之后必须**关编辑器整编再开**，Live Coding 看不到新栏。
- `WBP_NightHUD_Multi` 里的血条、背包如果挡住热区，UMG 会先吃触摸。热区只吃「没被 Widget 吃掉」的点。要对齐时把色块画出来，看有没有被控件盖住。
- 引擎虚拟摇杆在 `PossessedBy` 里 `ActivateTouchInterface(nullptr)` 关掉了，不要再往 GameMode / Pawn 上挂 Touch Interface。
- 以前那套固定 `NightHudPads` 色块已经去掉，默认画面不再画 Q/E 垫。

<a id="513-掉落飞图标"></a>

### 5.13 掉落飞图标

对应目录第 15 行。砍死怪之后，食材图标从击杀点飞进背包鱼嘴。映射不在这张手感表里：

```
DA_Course.FoeDropMap  →  EIngredientId  →  DT_Ingredients.Icon
```

当前正式五怪（改映射去 canonical `DA_Course`，不是 HUD）：

| 怪 | 食材 | 图标（`DT_Ingredients`） |
|---|---|---|
| M01 | 月鳞鱼 `YueLinYu` | `food_fish_V0` |
| M02 | 银山君 `YinShanJun` | `food_egg_V0` |
| M03 | 玄玉琴 `XuanYuQin` | `food_leg_V0` |
| M04 | 赤炎椒 `ChiYanJiao` | `food_hand_V0` |
| M05 | 灵谷 `LingGu` | `food_rice_V0` |

飞行终点是 `WBP_BagPack` 里的空盒子 `SB_FlyTarget`（拖到鱼嘴中心）。C++ 按名字找：`BagPackWidgetName = WBP_BagPack`，`BagFlyTargetWidgetName = SB_FlyTarget`。改名会静默退回 `BagFlyTargetFallback`。

打开 `BP_NightCourseHUD` → **类默认值** → `Night｜HUD｜Drop`：


| 属性                      | 默认    | 作用                            |
| ----------------------- | ----- | ----------------------------- |
| Enable Drop Fly Icons   | true  | 总开关                           |
| Drop Fly Seconds        | 0.7   | 一段飞行时长                        |
| Drop Fly Stagger Seconds| 0.09  | 一次掉多份时，后一张比前一张晚这么久出发          |
| Drop Fly Icon Size      | 200   | 起飞时边长（设计单位）                   |
| Drop Fly End Scale      | 0.35  | 到嘴时缩到起飞尺寸的这个比例                |
| Drop Fly Arc Height     | 300   | 弧线鼓起，正值往直线外侧拱                 |
| Drop Fly World Z Offset | 60    | 起点相对怪枢轴抬高，避免从图标脚底飞            |
| Max Drop Fly Icons      | 12    | 连砍时屏幕上最多同时几张                  |
| Bag Fly Target Fallback | (210, 150) | 找不到 `SB_FlyTarget` 时的备用终点 |


图标贴图来自 `DT_Ingredients` 的 Icon 列，表软引用默认 `/Game/Shared/Data/DT_Ingredients`。换图改表，不要把 Texture 硬填进 HUD。

`Ctrl+S` 后**停掉 PIE 再 Play**。Play 里改 `NightCourseHUD0` 是这一局，停了就丢。

<a id="514-跑酷音效"></a>

### 5.14 跑酷音效

对应目录第 16 行。成功斩击时 HUD 播三层：**刀** + **该怪的 Voice/Material** + **有掉落才播掉落**。资源在 `/Game/Night/Course/Audio/`。跳跃 / Miss 目前没有独立一击。

打开 `BP_NightCourseHUD` → **类默认值** → `Night｜HUD｜SFX`：


| 属性                           | 默认   | 作用                                      |
| ---------------------------- | ---- | --------------------------------------- |
| Enable Night Sfx             | true | 总开关                                     |
| Slash Sound                  | `SW_Slash` | 刀                                       |
| Ingredient Drop Sound        | `SW_IngredientDrop` | 掉落。源文件约 1.87s，默认只播开头                   |
| Ingredient Drop Play Seconds | 0.28 | 播这么久再淡出。**0 = 整段播完**                    |
| Ingredient Drop Fade Seconds | 0.12 | 截断时的淡出                                  |
| Slash / Foe Hit / Drop Volume| 1    | 三层各自音量。只静音掉落：把 Drop Volume 调 0          |
| Foe Hit Sounds               | 见下表  | 每怪 Voice + Material，任一槽可空               |


默认受击映射：

| 怪 | Voice | Material |
|---|---|---|
| M01 | `SW_Hit_Fish_Voice` | `SW_Hit_Fish_Material` |
| M02 | （空） | `SW_Hit_Bat_Material` |
| M03 / M04 | `SW_Hit_Aquatic_Voice` | （空） |
| M05 | `SW_Hit_Rice_Voice` | `SW_Hit_Rice_Material` |

动画通知轨上的 `Play Sound`（§5.10）是**另一路**：片子播到标记就出声，和这套 HUD 斩击音是两套。刀光 / 落地尘用通知；砍中这一下用 HUD。不要两边都挂同一记刀，会一刀两响。

`Ctrl+S` 后停 PIE 再 Play。

<a id="515-连击数字"></a>

### 5.15 连击数字

对应目录第 17 行。样子在 Widget 里，数字由 C++ 灌：`WBP_NightHUD_Multi` 里实例名必须是 `WBP_Combo`，里面的 TextBlock 必须叫 `Txt_ComboCount`。成功跳 / 砍 `Combo + 1`，按错 / 漏清零。**Combo 为 0 时整块 Collapsed**，开局看不见，成功一次才出。

位置、字体、「連」贴图：打开 `WBP_Combo` / `WBP_NightHUD_Multi` 在 Designer 里拖，不在 `DA_NightFeelTuning_Default`。搭法和坑见 `Docs/知识.md` §12。

**只有斩击成功 +1。** 跳跃成功出 `JUMP OK`，连击数字不动。按错 / Miss 清零。连击目前只驱动 UI / 特效档，**每 8 连回 4 魂还没接**，不要在手感表里找这项。

---



## 六、相关

- 裁定：`Docs/设计裁定.md`（R-006 扣魂口径、R-007 空档期起算点）
- 任务记录：`Docs/R1_任务清单.md`
- 夜关 HUD 搭法：`Docs/知识.md` §12
- 代码：`Source/MiniGame/Public/Night/Course/NightFeelTuningData.h`
- HUD 热区 / 掉落 / 音效：`Source/MiniGame/Public/Night/Course/NightCourseHUD.h`

